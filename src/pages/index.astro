---
// No need for frontmatter if nothing is imported
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Welcome</title>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen m-0 font-sans">
    <div class="bg-white p-10 rounded-2xl shadow-lg flex flex-col items-center gap-6 text-center">
      <h1 class="text-2xl font-semibold text-gray-800">Choose your language</h1>
      <button
        class="px-6 py-3 text-white bg-blue-600 hover:bg-blue-700 rounded-lg text-lg w-48"
        onclick="setLang('en')"
      >
        English
      </button>
      <button
        class="px-6 py-3 text-white bg-orange-500 hover:bg-orange-600 rounded-lg text-lg w-48"
        onclick="setLang('ar')"
      >
        العربية
      </button>
    </div>

    <script>
      const preferredLang = localStorage.getItem('lang');
      if (preferredLang === 'en') window.location.href = '/en';
      if (preferredLang === 'ar') window.location.href = '/ar';

      function setLang(lang) {
        localStorage.setItem('lang', lang);
        window.location.href = '/' + lang;
      }
    </script>
  </body>
</html>
